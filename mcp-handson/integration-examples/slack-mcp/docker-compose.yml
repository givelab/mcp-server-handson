version: "3.9"

services:
  mcp-server:
    build: .
    stdin_open: true
    tty: true
    env_file:
      - .env
    volumes:
      - ./logs:/app/logs
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
